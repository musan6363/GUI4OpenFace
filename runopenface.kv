#:kivy 2.0.0
RunWidget: # ルートに追加

<RunWidget>:
    BoxLayout:
        orientation: 'vertical'
        size: root.size

        # ラベル
        Label:
            id: label1
            font_size: 68
            text: root.label_text    # root(RunWidget)の変数テキストの値を取得
